# $ pip install Flask

start debug:

windows:
set FLASK_DEBUG=1
linux:
export FLASK_DEBUG=1

Linux:

cd ~/git/Client_leGluingMachineImpruvements/ && FLASK_APP=main.py flask run

* Running on http://localhost:5000/


Windows:

cd C:\Git\Client_SoleGluingMachineImpruvements && set FLASK_APP=main.py && flask run

* Running on http://127.0.0.1:5000/



to make it availiable in local network: 172.16.0.116:5000

cd C:\Git\Client_SoleGluingMachineImpruvements && set FLASK_APP=main.py && flask run --host=0.0.0.0