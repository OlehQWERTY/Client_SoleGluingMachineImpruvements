# $ pip install Flask

start debug:

windows:
set FLASK_DEBUG=1
linux:
export FLASK_DEBUG=1

Linux:

cd ~/git/Client_leGluingMachineImpruvements/ && FLASK_APP=main.py flask run

* Running on http://localhost:5000/


Windows:

cd C:\Git\Client_SoleGluingMachineImpruvements && set FLASK_APP=main.py && flask run

* Running on http://127.0.0.1:5000/



to make it availiable in local network: 172.16.0.116:5000

cd C:\Git\Client_SoleGluingMachineImpruvements && set FLASK_APP=main.py && flask run --host=0.0.0.0


# db how to:

cd C:\Git\Client_SoleGluingMachineImpruvements

python

from view import db

db.create_all()

from view import User, Post

user_1 = User(username='A_RIF_in', email='A_RIF_in@gmail.com', password='rif12345lolo')  # , usertype='admin'

db.session.add(user_1)

db.session.commit()  # commit to db

User.query.first() # show User

# result: User('A_RIF_in', 'A_RIF_in@gmail.com') 

 User.query.all()

User.query.filter_by(username='U_RIF_in')

User.query.filter_by(username='U_RIF_in').all()

User.query.filter_by(username='U_RIF_in').first()

user_1

user_1.id

# -----

user=User.query.get(1)

user

# db.drop_all() # del everything
# db.create_all() # recreate empty db


